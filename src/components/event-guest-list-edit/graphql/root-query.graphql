#import "./item-query.graphql"

query GuestLists($eventSlug: String) {
  viewer {
    id

    event(slug: $eventSlug) {
      id
      slug
      start

      guestLists {
        ...EventGuestListEditItemFragment
      }
    }
  }
}
