query EventPage($slug: String) {
  viewer {
    id

    event(slug: $slug) {
      id
      slug
      title
      flyer
      start
      end
      description
      price
      prices { description, value }
      directions
      place { id, name, coordinates, address, city { name } },
    }
  }
}
